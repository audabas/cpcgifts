<?xml version="1.0" encoding="utf-8"?>
<appengine-web-app xmlns="http://appengine.google.com/ns/1.0">
	<application>cpcgifts</application>
	<version>dev-14-07-07-objectify</version>

	<!-- Allows App Engine to send multiple requests to one instance in parallel: -->
	<threadsafe>true</threadsafe>

	<!-- Configure java.util.logging -->
	<system-properties>
		<property name="java.util.logging.config.file" value="WEB-INF/logging.properties" />
	</system-properties>

	<!-- HTTP Sessions are disabled by default. To enable HTTP sessions specify: -->

	<sessions-enabled>false</sessions-enabled>

	<!-- It's possible to reduce request latency by configuring your application 
		to asynchronously write HTTP session data to the datastore: -->

	<!-- <async-session-persistence enabled="true" /> -->

	<!-- With this feature enabled, there is a very small chance your app will 
		see stale session data. For details, see http://code.google.com/appengine/docs/java/config/appconfig.html#Enabling_Sessions -->

  	<automatic-scaling>
	    <max-idle-instances>1</max-idle-instances>
	    <min-pending-latency>10s</min-pending-latency>
	    <max-concurrent-requests>100</max-concurrent-requests>
	</automatic-scaling>
  	
	<static-files>
        <include path="/img/**" expiration="30d" />
        <include path="/css/**" expiration="5d" />
        <include path="/js/**" expiration="5d" />
        <include path="/favicon.ico" expiration="30d" />
        <include path="/**.html" expiration="30d" />
    </static-files>

	<warmup-requests-enabled>true</warmup-requests-enabled>
	
	<admin-console>
	  <page name="Admin database console" url="/adminconsole/index.jsp" />
	</admin-console>
	

</appengine-web-app>
